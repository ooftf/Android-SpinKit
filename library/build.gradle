apply plugin: 'com.android.library'
apply plugin: 'ooftf-maven'

android {
    compileSdkVersion 30
    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 30
    }

    lintOptions {
        abortOnError false
    }
}


publish {
    artifactId = 'loading-view'
    version = '0.0.1'
}